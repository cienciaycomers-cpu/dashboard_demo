# SDE implementation log: paid-media-dashboard

## T1 — baseline and toolchain

Completed the React + TypeScript + Vite baseline with strict TypeScript settings, local build scripts and Vitest.

Files created:

- `package.json`
- `package-lock.json` generated by npm install
- `tsconfig.json`
- `tsconfig.app.json`
- `tsconfig.node.json`
- `vite.config.ts`
- `index.html`
- `src/main.tsx`
- `src/App.tsx`
- `src/styles.css`

Verification:

- `npm run typecheck` passed.
- `npm test -- --passWithNoTests` passed with no test files yet.
- `npm run build` passed.

Notes:

- npm reported three dependency audit findings after installation. No forced audit fix was applied.
- The initial App component is intentionally a minimal baseline and will be replaced by the planned dashboard shell after domain work is verified.

## T2 — dataset contract and safe fixture

Completed the typed domain contract and deterministic 12-month demo fixture.

Files created:

- `src/domain/types.ts`
- `src/data/demoData.ts`
- `src/data/demoData.test.ts`

Behavior:

- One row per date, platform and demo campaign.
- Six generic Google/Meta campaign profiles.
- Current demo date is `2026-07-22` with a 12-month window from `2025-08-01`.
- Missing revenue/conversion inputs remain `null` on a designated row.
- A controlled spend/conversion anomaly is included for alert validation.
- No source brand or campaign identifier is included.

Verification:

- Type-check passed.
- Three dataset tests passed.

## T3 - metric engine

Completed pure metric calculations in `src/domain/metrics.ts`.

Covered metrics:

- CTR, CPC, conversion rate and CPA.
- ROAS and ACOS on gross attributed revenue.
- Net revenue, contribution margin and margin after ads.
- Dynamic break-even ACOS using contribution margin before ads.

Missing input and zero denominator states are explicit. Three formula tests pass.

## T4 - period comparison and anomaly engine

Completed `src/domain/analysis.ts` with:

- available month discovery;
- selected-month window handling;
- same-elapsed-days previous-month comparison;
- percentage variation states;
- ACOS, no-conversion, spend-change and missing-revenue alerts.

Three analysis tests pass. The controlled daily anomaly is intentionally stronger than the 10% target when isolated so the alert path is verifiable.

## T5-T8 - dashboard implementation

Completed the interactive dashboard shell and view composition in `src/App.tsx` with the premium dark visual system in `src/styles.css`.

Implemented:

- current-month overview and calendar-month selector;
- same-elapsed-days previous-period comparison;
- platform, campaign and objective filters;
- KPI cards for spend, gross revenue, ACOS, ROAS, net revenue and contribution margin;
- inline trend visualization;
- acquisition, conversion and profitability sections;
- campaign detail table and contextual detail drawer;
- anomaly list with informative, warning and critical states;
- explicit `N/D` treatment for missing metrics;
- responsive layouts for narrower screens.

Verification after UI integration:

- `npm run typecheck` passed.
- `npm test -- --run` passed: three files, ten tests.
- `npm run build` passed.
